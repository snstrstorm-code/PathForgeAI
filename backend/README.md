# PathForge AI - Recommendation & NLP Engine

This module handles skill extraction from resumes/projects and computes user skill profiles (Strong, Developing, Critical Gap).

## Setup Instructions

1. **Install Dependencies:**
   ```bash
   pip install -r requirements.txt
   python -m spacy download en_core_web_sm
