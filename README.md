# PathForge AI - Recommendation & NLP Engine
This folder contains the AI components for extracting skills from resumes and calculating skill gaps.

## How to test:
1. Install requirements: `pip install -r requirements.txt`
2. Run extraction: `python main.py`
